<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use OpenAI;

class ChatGptControllers extends Controller
{
    //  // Set up the API key
    // OpenAI::setApiKey('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');

    // // Define the prompt to send to GPT-3
    // $prompt = "Hello, GPT-3!";

    // // Set up the API parameters
    // $params = [
    //     "prompt" => $prompt,
    //     "temperature" => 0.7,
    //     "max_tokens" => 60,
    // ];

    // // Send the API request
    // $response = OpenAI::complete($params);

    // // Extract the generated text from the response
    // $generated_text = $response['choices'][0]['text'];

    // // Return the generated text as a response
    // return response($generated_text);

}
